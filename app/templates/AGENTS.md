# Template Guidelines

- Use Jinja macros from `components.html` to keep markup consistent.
- Follow `docs/style_guide.md` for accessible HTML.
- Format template files with `prettier`.
- Update tests in `tests/test_html_templates.py` if templates change.

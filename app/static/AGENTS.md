Glanceable Ambient UI – dark-first, minimal controls, gesture driven.
Tailwind only; forbid inline styles & !important; prefer @apply in .css.
Place images in `img`, scripts in `js`, and styles in `css`.
Run `prettier` on JS and CSS files.
Keep asset filenames stable; update `manifest.json` if new files are added.

# Group Dropdown Navigation

When many camera groups exist, individual links crowd the header. The navigation bar now uses a dropdown list so you can jump directly to any group. The current group is pre‑selected when viewing its page and selecting **All Groups** returns to the dashboard. The dropdown remains accessible within the mobile hamburger menu.
